$("img");              // get all images
$(".card_image");      // get all elements with class .card_image
$("#my_footer");       // get the footer by ID
$("footer p");         // get all pars within footer
$(":header");          // get all headers - h1 - h6
$(":first");           // get the first element
$(":last");            // get the last element
$("last:img");         // get the last image
$("[href]");           // get all elements with an attribute of 'href'